Please create a Playwright test in TypeScript for the BDD feature provided in the <feature> block below. 
Rely on the provided <html_context> block to find accurate locators.

High-Level Framework Standards:
- Use the Page Object Model (POM) architecture.
- Write clean, modern TypeScript.
- Use Playwright web-first assertions.

OUTPUT FORMAT (CRITICAL):
Do NOT output any conversational text, greetings, or explanations. 
You MUST output EXACTLY two files. You MUST prepend each file with the exact delimiter "### FILE: " followed by the file path. Wrap the code in standard markdown TypeScript blocks.

### FILE: pageObjects/{test_name}Page.ts
```typescript
// Complete Page Object Model class goes here